<h1>New Post</h1>

<%= form_for :post, url: posts_path do |f| %>
<ul>
	<li>
		<%= f.label :title %>
		<%= f.text_field :title %>
	</li>
	<li>
		<%= f.label :body %>
		<%= f.text_area :body %>
	</li>
</ul>
<p>
	<%= f.submit %>
</p>
<% end %>

<%= link_to 'back', admin_path, class: 'btn btn-info' %>